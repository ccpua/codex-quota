# 需求与界面检查 · 2026-09-08

## 已验证

- 额度解析：周周期、双周期、缺失数据、旧格式回退、范围边界。
- 菜单栏：取剩余比例更低的周期；离线时保留读数并加警告。
- 重置倒计时：临近重置、已过期与无重置时间。
- 浮窗布局：左右展开、屏幕边缘、超出屏幕的位置恢复、收起后保持上沿。
- 独立运行：启动或退出 Codex 不影响额度工具；安装脚本会清理旧版自动启动监听器。
- 视觉：36 张胶囊/详情预览（系统浅/深色下的正常、双周期、低额度、0%、100%、初次加载、离线、未登录和刷新中）。检查控件边界、单行文字宽度和刷新按钮状态。
- 人工查看离线预览：默认胶囊 96×34 pt；详情 280×216 pt（单周期）；深色细边框，简化工具栏，无系统标题栏。
- 图标：`icon.svg` 渲染为 2048×2048 RGBA PNG，含深色圆角 tile、薄荷色额度环、琥珀检查点和几何 Codex 火花；已写入 app `Contents/Resources/AppIcon.png`，构建签名检查通过。

## 本轮修复

- 原浮窗默认过大，改为按钮尺寸胶囊，悬停展开、移开延迟收起。
- 移除展开/收起动画，改为即时切换；悬停延迟、屏幕边界和拖动定位保持不变。
- 去掉重复标题与底部大按钮，采用精简深色界面；数值、日期、更新状态分层显示。
- 补充重置倒计时、置顶偏好保存、胶囊位置保存与屏幕边界处理。
- “更多”菜单在按钮旁打开；跟踪菜单时不自动收起。
- 升级安装保留原先的运行/关闭状态，安装前验证新构建并支持失败恢复。
- 连接检查失败时返回非零状态；SIGTERM 正常清理独立额度连接。

## 仍待真实交互确认

Computer Use 当前未获授权，因此本轮尚未通过屏幕操作验证悬停切换、拖动、按钮点击、菜单栏位置或真实 Codex 退出/重启。没有为了测试而退出正在承载此任务的 Codex。

离线预览与逻辑测试不等同于真实鼠标交互验收。需要电脑操作权限后继续核对，或由用户亲自悬停、移开和点击确认。
